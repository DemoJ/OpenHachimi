app:
  default_role: default
  browser_headless: true     # 是否无头模式运行浏览器（开发测试时建议 false，后台服务建议 true）
  browser_channel: ""        # 本地浏览器的通道，如果安装了 Chrome，可以填 "chrome" 或 "msedge"，无需安装内核。如果留空，则使用 Playwright 默认的 Chromium
  telegram_bot_token: ""     # Telegram Bot Token，通过 @BotFather 申请后填入。留空则不启用 Telegram 渠道
  telegram_proxy_url: ""     # 代理地址（可选），在无法直连 Telegram 的环境下配置。支持 HTTP/SOCKS5，例如：
                             #   socks5://127.0.0.1:1080
                             #   http://192.168.1.1:7890

llm:
  api_key: sk-xxxxxxxx
  model: gpt-5.2
  base_url: https://your-openai-compatible-server.example/v1

paths:
  roles_dir: user/roles
  memory_dir: .memory

logging:
  level: INFO
  dir: .logs
  console: false
